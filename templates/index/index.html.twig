{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
Video Downloader v 0.1
{{ form(form) }}

{% for link in links %}

    <a href="{{ link.url }}" target="_blank">{{ link.url }}</a></br>
    {{ link.itag }}
    {{ link.format }}</br>

{% endfor %}
</div>

{% endblock %}
